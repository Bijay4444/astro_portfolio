---
import SectionHeading from "@/components/SectionHeading.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const entries = (await getCollection("library")).filter((entry) => entry.data.type === "thought");
---

<BaseLayout title="Thoughts | Bijay Khadka" description="Short thoughts from Bijay Khadka.">
  <SectionHeading eyebrow="Library" title="Thoughts" />
  <div class="grid gap-4">
    {entries.map((entry) => (
      <a href={`/library/${entry.id}`} class="deco-card block p-6">
        <p class="deco-kicker">Thought</p>
        <h2 class="mt-3 font-display deco-section-title text-2xl text-[var(--text)]">{entry.data.title}</h2>
        <p class="mt-2 text-[var(--muted-text)]">{entry.data.description}</p>
      </a>
    ))}
  </div>
</BaseLayout>

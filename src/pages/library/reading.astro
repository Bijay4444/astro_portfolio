---
import SectionHeading from "@/components/SectionHeading.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const entries = (await getCollection("library")).filter((entry) => entry.data.type === "reading");
---

<BaseLayout title="Reading List | Bijay Khadka" description="Reading links from Bijay Khadka's portfolio library.">
  <SectionHeading eyebrow="Library" title="Reading List" />
  <div class="grid gap-4">
    {entries.map((entry) => (
      <a href={`/library/${entry.id}`} class="deco-card block p-6">
        <p class="deco-kicker">Reading</p>
        <h2 class="mt-3 font-display deco-section-title text-2xl text-[var(--text)]">{entry.data.title}</h2>
        <p class="mt-2 text-[var(--muted-text)]">{entry.data.description}</p>
      </a>
    ))}
  </div>
</BaseLayout>
